# AMT containers connect only through the project-owned Unix socket.
# Password authentication remains mandatory so credential rotation is effective.
local all all scram-sha-256
host all all 127.0.0.1/32 scram-sha-256
host all all ::1/128 scram-sha-256
